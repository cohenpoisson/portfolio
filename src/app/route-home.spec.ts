import { RouteHome } from './route-home';

describe('RouteHome', () => {
  it('should create an instance', () => {
    const directive = new RouteHome();
    expect(directive).toBeTruthy();
  });
});
